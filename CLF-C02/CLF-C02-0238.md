# Câu hỏi #238

A company wants to grant a new employee long-term access to manage Amazon DynamoDB databases. Which of the following is a recommended best-practice when granting these permissions?

## Options

**A.** Create an IAM role and attach a policy with Amazon DynamoDB access permissions.
**B.** Create an IAM role and attach a policy with Administrator access permissions.
**C.** Create an IAM user and attach a policy with Amazon DynamoDB access permissions.
**D.** Create an IAM user and attach a policy with Administrator access permissions.


---

## 1. CONTEXT & ĐỀ BÀI
- **Scenario:** Tình huống được mô tả: _A company wants to grant a new employee long-term access to manage Amazon DynamoDB databases. Which of the following is ..._
- **Existing Resources:** Đề nhắc tới: `DynamoDB` — dịch vụ NoSQL key-value được quản lý hoàn toàn; `IAM` — Identity and Access Management - quản lý quyền truy cập.
- **Current Issue/Goal:** Lựa chọn giải pháp bảo mật tốt nhất theo best practice của AWS.

## 2. KEYWORDS QUAN TRỌNG
| Keyword | Ý nghĩa / Gợi ý |
|---------|-----------------|
| ``DynamoDB`` | dịch vụ NoSQL key-value được quản lý hoàn toàn |
| ``IAM`` | Identity and Access Management - quản lý quyền truy cập |

## 3. YÊU CẦU CỦA ĐỀ
- **Question type:** Most secure / Security best practice
- **Constraints:** Bảo mật dữ liệu và quyền truy cập

## 4. ĐÁP ÁN ĐÚNG
**✅ Đáp án: C**

**Giải thích:**
Đáp án `DynamoDB` đúng vì dịch vụ NoSQL key-value được quản lý hoàn toàn, phù hợp với yêu cầu của đề.
Trong ngữ cảnh này, `DynamoDB` đóng vai trò: lưu trữ dữ liệu phi quan hệ với độ trễ thấp ở mọi quy mô.

## 5. CÁC ĐÁP ÁN SAI
**❌ Đáp án A:**
- Tại sao sai: `DynamoDB` (dịch vụ NoSQL key-value được quản lý hoàn toàn) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu bảo mật chính là lưu trữ dữ liệu phi quan hệ với độ trễ thấp ở mọi quy mô này.

**❌ Đáp án B:**
- Tại sao sai: `IAM` (Identity and Access Management - quản lý quyền truy cập) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu bảo mật chính là quản lý user, group, role, policy cho AWS resources này.

**❌ Đáp án D:**
- Tại sao sai: `IAM` (Identity and Access Management - quản lý quyền truy cập) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu bảo mật chính là quản lý user, group, role, policy cho AWS resources này.

## 6. MẸO GHI NHỚ (Memory Hook)
🧠 *Đáp án **C** = `DynamoDB`. Bảo mật = defense-in-depth: nhiều lớp, nguyên tắc least privilege.*
