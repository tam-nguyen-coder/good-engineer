# Câu hỏi #86

AWS has created a large number of Edge Locations as part of its Global Infrastructure. Which of the following is NOT a benefit of using Edge Locations?

## Options

**A.** Edge locations are used by CloudFront to cache the most recent responses.
**B.** Edge locations are used by CloudFront to improve your end users' experience when uploading files.
**C.** Edge locations are used by CloudFront to distribute traffic across multiple instances to reduce latency.
**D.** Edge locations are used by CloudFront to distribute content to global users with low latency.


---

## 1. CONTEXT & ĐỀ BÀI
- **Scenario:** Ngữ cảnh đề bài: _AWS has created a large number of Edge Locations as part of its Global Infrastructure. Which of the following is NOT a b..._
- **Existing Resources:** Không có tài nguyên cụ thể — câu hỏi thuộc dạng lý thuyết/khái niệm.
- **Current Issue/Goal:** Lựa chọn giải pháp có hiệu năng cao nhất, độ trễ thấp nhất.

## 2. KEYWORDS QUAN TRỌNG
| Keyword | Ý nghĩa / Gợi ý |
|---------|-----------------|
| `Performance` | Yêu cầu hiệu năng, độ trễ thấp |
| `Global / Multi-region` | Phạm vi toàn cầu, nhiều region |
| ``CloudFront`` | CDN (Content Delivery Network) toàn cầu |

## 3. YÊU CẦU CỦA ĐỀ
- **Question type:** Best performance / Lowest latency
- **Constraints:** Hiệu năng và độ trễ thấp

## 4. ĐÁP ÁN ĐÚNG
**✅ Đáp án: C**

**Giải thích:**
Đáp án `CloudFront` đúng vì CDN (Content Delivery Network) toàn cầu, phù hợp với yêu cầu của đề.
Trong ngữ cảnh này, `CloudFront` đóng vai trò: phân phối nội dung tĩnh/động qua edge location, giảm latency.

## 5. CÁC ĐÁP ÁN SAI
**❌ Đáp án A:**
- Tại sao sai: `CloudFront` (CDN (Content Delivery Network) toàn cầu) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu phân phối nội dung tĩnh/động qua edge location, giảm latency được ưu tiên hơn hiệu năng.

**❌ Đáp án B:**
- Tại sao sai: `CloudFront` (CDN (Content Delivery Network) toàn cầu) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu phân phối nội dung tĩnh/động qua edge location, giảm latency được ưu tiên hơn hiệu năng.

**❌ Đáp án D:**
- Tại sao sai: `CloudFront` (CDN (Content Delivery Network) toàn cầu) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng khi yêu cầu phân phối nội dung tĩnh/động qua edge location, giảm latency được ưu tiên hơn hiệu năng.

## 6. MẸO GHI NHỚ (Memory Hook)
🧠 *Đáp án **C** = `CloudFront`. Hiệu năng = edge location + cache + chọn instance/region gần user.*
