# Câu hỏi #249

Which of the following are true regarding the languages that are supported on AWS Lambda? (Choose TWO)

## Options

**A.** Lambda only supports Python and Node.js, but third party plugins are available to convert code in other languages to these formats.
**B.** Lambda natively supports a number of programming languages such as Node.js, Python, and Java.
**C.** Lambda is AWS' proprietary programming language for microservices.
**D.** Lambda doesn't support programming languages; it is a serverless compute service.
**E.** Lambda can support any programming language using an API.


---

## 1. CONTEXT & ĐỀ BÀI
- **Scenario:** Ngữ cảnh đề bài: _Which of the following are true regarding the languages that are supported on AWS Lambda? (Choose TWO)_
- **Existing Resources:** Đề nhắc tới: `Lambda` — dịch vụ serverless compute chạy theo sự kiện.
- **Current Issue/Goal:** Xác định đúng khái niệm/dịch vụ/tính năng AWS được hỏi tới.

## 2. KEYWORDS QUAN TRỌNG
| Keyword | Ý nghĩa / Gợi ý |
|---------|-----------------|
| `Serverless` | Không muốn quản lý server |
| `Web application` | Ứng dụng web/API |
| ``Lambda`` | dịch vụ serverless compute chạy theo sự kiện |

## 3. YÊU CẦU CỦA ĐỀ
- **Question type:** Compute
- **Constraints:** Tuân theo best practice AWS

## 4. ĐÁP ÁN ĐÚNG
**✅ Đáp án: B và E**

**Giải thích:**
Đề yêu cầu chọn nhiều đáp án. Hai phương án **B** và **E** cùng đáp ứng các tiêu chí của câu hỏi.
- **B** — `Lambda`: dịch vụ serverless compute chạy theo sự kiện.
- **E** — `Lambda`: dịch vụ serverless compute chạy theo sự kiện.

## 5. CÁC ĐÁP ÁN SAI
**❌ Đáp án A:**
- Tại sao sai: `Lambda` (dịch vụ serverless compute chạy theo sự kiện) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `Lambda`.

**❌ Đáp án C:**
- Tại sao sai: `Lambda` (dịch vụ serverless compute chạy theo sự kiện) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `Lambda`.

**❌ Đáp án D:**
- Tại sao sai: `Lambda` (dịch vụ serverless compute chạy theo sự kiện) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `Lambda`.

## 6. MẸO GHI NHỚ (Memory Hook)
🧠 *Đáp án **B và E** = `Lambda` — phù hợp với yêu cầu đề bài.*
