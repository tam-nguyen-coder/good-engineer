# Câu hỏi #280

Which of the below options is true of Amazon VPC?

## Options

**A.** Amazon VPC allows customers to control user interactions with all other AWS resources.
**B.** AWS Customers have complete control over their Amazon VPC virtual networking environment.
**C.** AWS is responsible for all the management and configuration details of Amazon VPC.
**D.** Amazon VPC helps customers to review their AWS architecture and adopt best practices.


---

## 1. CONTEXT & ĐỀ BÀI
- **Scenario:** Đề hỏi về khái niệm/dịch vụ AWS liên quan tới: _Which of the below options is true of Amazon VPC?_
- **Existing Resources:** Đề nhắc tới: `VPC` — Virtual Private Cloud - mạng riêng ảo trong AWS.
- **Current Issue/Goal:** Lựa chọn đáp án đúng nhất theo yêu cầu của đề.

## 2. KEYWORDS QUAN TRỌNG
| Keyword | Ý nghĩa / Gợi ý |
|---------|-----------------|
| ``VPC`` | Virtual Private Cloud - mạng riêng ảo trong AWS |

## 3. YÊU CẦU CỦA ĐỀ
- **Question type:** Networking / DNS / CDN
- **Constraints:** Tuân theo best practice AWS

## 4. ĐÁP ÁN ĐÚNG
**✅ Đáp án: B**

**Giải thích:**
Đáp án `VPC` đúng vì Virtual Private Cloud - mạng riêng ảo trong AWS, phù hợp với yêu cầu của đề.
Trong ngữ cảnh này, `VPC` đóng vai trò: cô lập tài nguyên mạng, kiểm soát subnet, routing, security group.

## 5. CÁC ĐÁP ÁN SAI
**❌ Đáp án A:**
- Tại sao sai: `VPC` (Virtual Private Cloud - mạng riêng ảo trong AWS) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `VPC`.

**❌ Đáp án C:**
- Tại sao sai: `VPC` (Virtual Private Cloud - mạng riêng ảo trong AWS) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `VPC`.

**❌ Đáp án D:**
- Tại sao sai: `VPC` (Virtual Private Cloud - mạng riêng ảo trong AWS) không phù hợp với yêu cầu cụ thể của đề.
- Khi nào đúng: Đáp án này sẽ đúng trong tình huống khác yêu cầu cụ thể `VPC`.

## 6. MẸO GHI NHỚ (Memory Hook)
🧠 *Đáp án **B** = `VPC` — phù hợp với yêu cầu đề bài.*
