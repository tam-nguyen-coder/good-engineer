# Question #523 - Topic 1

A company runs a microservice-based serverless web application. The application must be able to retrieve data from multiple Amazon DynamoDB tables A solutions architect needs to give the application the ability to retrieve the data with no impact on the baseline performance of the application. Which solution will meet these requirements in the MOST operationally efficient way?

## Options

**A.** AWS AppSync pipeline resolvers

**B.** Amazon CloudFront with Lambda@Edge functions

**C.** Edge-optimized Amazon API Gateway with AWS Lambda functions

**D.** Amazon Athena Federated Query with a DynamoDB connector

## 1. CONTEXT & ĐỀ BÀI
- **Scenario:** Serverless microservice web app cần retrieve data từ nhiều DynamoDB tables mà không ảnh hưởng performance.
- **Existing Resources:** Serverless web application, nhiều DynamoDB tables.
- **Current Issue/Goal:** Truy xuất nhiều DynamoDB tables hiệu quả, không impact baseline performance.

## 2. KEYWORDS QUAN TRỌNG
| Keyword | Ý nghĩa / Gợi ý |
|---------|-----------------|
| `retrieve data from multiple DynamoDB tables` | Cần gộp dữ liệu từ nhiều nguồn |
| `no impact on baseline performance` | Giải pháp không làm chậm ứng dụng |
| `AppSync pipeline resolvers` | Cho phép chain nhiều resolvers để query nhiều nguồn dữ liệu trong một request |
| `most operationally efficient` | Serverless, managed service, ít code nhất |

## 3. YÊU CẦU CỦA ĐỀ
- **Question type:** Most operationally efficient
- **Constraints:** No impact on baseline performance, microservice-based serverless

## 4. ĐÁP ÁN ĐÚNG
**✅ Đáp án: A**

**Giải thích:**
- **Sức mạnh của GraphQL và AWS AppSync:** AWS AppSync là dịch vụ serverless được quản lý hoàn toàn để xây dựng các API GraphQL. Một trong những lợi thế cốt lõi của GraphQL là cho phép client truy vấn và tổng hợp dữ liệu từ nhiều nguồn khác nhau (ở đây là nhiều bảng DynamoDB) chỉ trong một lượt gọi API duy nhất (single request).
- **AppSync Pipeline Resolvers:** Tính năng này cho phép bạn định nghĩa các bộ giải quyết (resolvers) theo chuỗi (pipeline), gồm nhiều chức năng (functions) được thực thi tuần tự hoặc song song. Mỗi function có thể kết nối trực tiếp với một bảng DynamoDB khác nhau. AppSync sẽ tự động kết nối và gom dữ liệu từ các bảng này mà không cần trung gian tính toán, đảm bảo **không ảnh hưởng đến hiệu năng nền tảng (no impact on baseline performance)** của ứng dụng.
- **Tối ưu hóa vận hành (Most operationally efficient):** Vì là giải pháp native tích hợp trực tiếp giữa AppSync và DynamoDB, bạn không cần phải viết, cấu hình hay duy trì các đoạn mã AWS Lambda phức tạp để làm nhiệm vụ điều phối dữ liệu (orchestration). Mọi thứ được định nghĩa trực tiếp qua GraphQL schema và Resolver mapping templates (VTL hoặc JavaScript).

## 5. CÁC ĐÁP ÁN SAI
**❌ Đáp án B:**
- **CloudFront + Lambda@Edge:** Lambda@Edge được thiết kế để xử lý các logic siêu nhẹ tại các Edge Location của mạng phân phối nội dung CDN (như tùy biến header, chuyển hướng URL). Việc gọi và tổng hợp dữ liệu từ nhiều bảng DynamoDB từ Edge sẽ gặp độ trễ cao, giới hạn thời gian chạy (timeout) thấp và không tối ưu cho kiến trúc microservices.

**❌ Đáp án C:**
- **API Gateway + Lambda:** Mặc dù đây là mô hình serverless phổ biến, nhưng để tổng hợp dữ liệu từ nhiều bảng DynamoDB, bạn phải tự viết code điều phối (orchestration code) trong Lambda function để gọi song song/tuần tự đến các bảng đó bằng AWS SDK. Điều này làm tăng độ phức tạp của code, tăng gánh nặng bảo trì (operational overhead) và có nguy cơ ảnh hưởng hiệu năng do độ trễ khởi động lạnh (cold starts) của Lambda.

**❌ Đáp án D:**
- **Amazon Athena Federated Query:** Athena dùng để thực hiện các truy vấn SQL phân tích (ad-hoc analytics) trên nhiều nguồn dữ liệu khác nhau. Tốc độ truy vấn của Athena mất từ vài giây đến vài chục giây, hoàn toàn không đáp ứng được yêu cầu về hiệu năng thời gian thực (real-time/OLTP) của một ứng dụng web và sẽ gây ảnh hưởng nghiêm trọng đến baseline performance.

## 6. MẸO GHI NHỚ (Memory Hook)
🧠 *"Query nhiều bảng DynamoDB trong 1 request với hiệu năng cao nhất → Chọn AWS AppSync Pipeline Resolvers (gộp dữ liệu native bằng GraphQL). Tránh dùng Lambda tự viết code điều phối hoặc dùng Athena SQL chậm chạp."*
